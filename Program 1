Program to access digits of a given integer and print them 

#include <stdio.h>

int main()
{
    int x,y;
    printf("Enter the number\n");
    scanf("%d",&x);
    
    printf("Digits\n");
    while(x>0)
    {
        y=x%10;
        printf("%d\n",y);
        x=x/10;
        
    }
    return 0;
}
